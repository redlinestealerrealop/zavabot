[project]
name = "zavascriptloader-bot"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "discord.py>=2.4.0",
    "psutil>=5.9.0",
    "python-dotenv>=1.0.0",
    "aiohttp",
    "fastapi",
]

[tool.uv.workspace]
members = [
    "api",
]
